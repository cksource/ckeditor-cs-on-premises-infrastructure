apiVersion: v1
kind: Service
metadata:
  name: {{ include "ckeditor-cs.fullname" . }}
  labels:
    {{- include "ckeditor-cs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: tcp
      protocol: TCP
      name: tcp
  selector:
    {{- include "ckeditor-cs.selectorLabels" . | nindent 4 }}
