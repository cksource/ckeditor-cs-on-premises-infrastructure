{
   "getCredentials": "Getting the credentials",
   "validateCredentials": "Validating the credentials",
   "validateEnvironment": "Validating the environment",
   "dockerAuth": "Docker registry authorization",
   "dockerPull": "Pulling the docker image",
   "editFile": "Editing docker-compose.yml file",
   "dockerUpWaiting": "Starting the docker containers (it may take several minutes)",
   "dockerUp": "Starting the docker containers",
   "createEnvironment": "Creating a new environment"
}